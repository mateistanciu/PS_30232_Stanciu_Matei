package models;

public class Admin extends Account{

	public Admin(String firstName, String lastName, String email, String username, String password, String role) {
		super(firstName, lastName, email, username, password, role);
		
	}
}
